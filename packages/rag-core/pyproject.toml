[project]
name = "filesense-rag-core"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["langgraph>=1,<2"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/desktop_agent_rag_core"]
